apiVersion: v1
kind: Service
metadata:
  name: ci-cd-service
spec:
  type: NodePort
  selector: 
    app: ci-cd-demo
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30007

